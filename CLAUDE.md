@AGENTS.md

## Claude Code

- Treat `AGENTS.md` as the canonical shared project instructions for this repo.
- Put private or machine-specific notes in `CLAUDE.local.md`; do not commit
  them.
- Keep proposed edits focused and verify Emacs changes with `make compile` and
  `make test` when practical.
